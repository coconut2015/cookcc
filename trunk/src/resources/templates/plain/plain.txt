# properties
bol = $bol$
backup = $backup$
cases = $cases$
$if(ecs)$
# ecs
$ecs;separator=","$
$endif$

# dfa
$dfa;separator="\n"$
# lexer states
$lexerStates;separator=","$
# lexer begins
$lexerBegins;separator=","$
# end
# statistics:
$statistics;format="# "$
